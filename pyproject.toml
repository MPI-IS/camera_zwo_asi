[build-system]
requires = [
    "wheel",
    "setuptools>=75",
    "setuptools_scm[toml]>=6.0",
    "cmake>=3.27",
    "cmake_build_extension",
    "pybind11>=2.12",
    "ninja",
]
build-backend = "setuptools.build_meta"
